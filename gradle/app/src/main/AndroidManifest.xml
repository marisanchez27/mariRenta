<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mari.marirenta">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MariRenta">
        <activity
            android:name=".VerifyEmail"
            android:label="@string/app_name"
            android:theme="@style/Theme.MariRenta">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".VerifyPassword"
            android:theme="@style/Theme.MariRenta.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.MariRenta.NoActionBar" />
        <activity
            android:name=".AddUser"
            android:theme="@style/Theme.MariRenta.NoActionBar" />
        <activity
            android:name=".DetailsUser"
            android:theme="@style/Theme.MariRenta.NoActionBar" />
    </application>

</manifest>